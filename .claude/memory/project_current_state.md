---
name: ShareWorkin v1.07 現在地
description: v1.07修正済み（0505テストフィードバック反映・2層データアーキテクチャ導入）。インストーラー未生成・実機テスト待ち。
type: project
---

## 現在地（2026-05-05 Win10セッション）

**バージョン**: v1.07（main にコミット済み `621a9b2`）

**本日の修正（0505テストフィードバック）**:

### 02.png 対応
- SMBパス表示（`¥¥WIN11¥ShareWorkin共有`）を削除
- 再読み込みボタンのアイコン縮小（FontSize 16→12）
- ショップルートに保留フォルダーを常時表示（`C:\MyApps\ShareWorkin\hold` のまま権限保護）

### 03-04.png 対応（2層データアーキテクチャ導入）
- `SwkNetworkCache`（新規）: アプリ内キャッシュ層を確立
  - `Candidates` / `ShopInfos` / `LastScanAt` / `ScanMode` を保持
  - `RefreshAsync()` がスキャン〜更新を一括担当
- `LanScanner`: `fullScan` パラメーター追加（ポート21/22でインストール候補検出）
- `UserListWindow`: スキャンモード選択UI（接続可能スキャン / 全PCスキャン）追加
- `UserListWindow`: 「再スキャン」に改名・キャッシュ読み取り構造に変更
- フッター表示: 接続可能スキャン→「接続可能 N」/ 全PCスキャン→「接続可能 N / その他 N」
- `UserListRowKind.InstallCandidate`（Tier5）追加
- `MainWindow`: 起動時バックグラウンドQuickスキャン開始

### 05.png 対応
- `FriendsWindow`: ボタン行を `UniformGrid` 全幅均等割りに変更（ボタン増加にも対応）
- `FriendsWindow`: ステータスをボタン行の下に独立配置

## テスト状況
- コミット・ビルド確認済み
- インストーラー未生成
- **次回**: インストーラー生成 → 両PC再インストール → 実機テスト

## 後半戦仕様書
- `_works/ShareWorkin_共有エクスプローラー仕様書_v0_1.md` 作成済み（2026-05-05）
- 着手はインストーラー最終確認を経てから。西村さんから依頼が来る

## 残課題（02-05以外）
| # | 内容 |
|---|------|
| 1 | パスワードキャンセル後もプロセスが残存 |
| 2 | UIフロー強調（選ぶ→お店を開く→ユーザー一覧の順に点灯） |
| 3 | 処理中の「考え中」表示（0.5秒以上の操作） |
| 4 | 再読み込みボタン自動点灯（ネットワーク変化検知） |
| 5 | デバイス名オレンジ化が出ないことがある |
| 6 | ピックアップボタン選択時に点灯 |
| 7 | ピックアップ画面タイトル復活・ステータス2項目1行化 |
| 8 | アクションガイドにデバイス名＋IPアドレス表示 |
| 9 | 新規接続フォーム改善（共有フォルダー名表示・権限項目削除・状態表示整理） |
| 10 | 確定ボタン（現「OK」→「確定」） |
| 幽霊 | タスクトレイアイコン重複（ShareWorkin.iss修正待ち） |
| 共有アクション | フォルダーを開けない件 |

**How to apply:** 次回セッション開始時に最新ログとこのメモを確認してから着手。
