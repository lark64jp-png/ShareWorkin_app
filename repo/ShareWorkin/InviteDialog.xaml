<Window x:Class="ShareWorkin.InviteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="お友達を招く"
        Height="440"
        Width="540"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F5F7FA"
        FontFamily="Yu Gothic UI">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="6" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="*" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="お友達を招く"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="#111827" />
        <TextBlock x:Name="HintTextBlock"
                   Grid.Row="2"
                   FontSize="12"
                   Foreground="#4B5563"
                   TextWrapping="Wrap" />

        <Border Grid.Row="4"
                BorderBrush="#D7DEE8"
                BorderThickness="1"
                CornerRadius="6"
                Background="White"
                Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="TokenTextBox"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         FontFamily="Consolas"
                         FontSize="12"
                         BorderThickness="0"
                         Background="Transparent" />
            </ScrollViewer>
        </Border>

        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="6"
                   FontSize="12"
                   Foreground="#4B5563" />

        <StackPanel Grid.Row="8"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="CopyButton"
                    Content="コピー"
                    Width="100"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="CopyButton_Click" />
            <Button x:Name="SaveSheetButton"
                    Content="シートで保存"
                    Width="120"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="SaveSheetButton_Click" />
            <Button Content="閉じる"
                    Width="84"
                    Height="32"
                    IsCancel="True"
                    Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Window>
